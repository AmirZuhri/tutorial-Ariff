const name = 'Bob';
const now = new Date();
console.log(`${name} - ${now.toLocaleString()}`);

const names = ['Harris', 'Bala', 'Charlie'];  
for (const eachName of names) {             //for loop example. forgot to open with () after for
        console.log(eachName);
    }